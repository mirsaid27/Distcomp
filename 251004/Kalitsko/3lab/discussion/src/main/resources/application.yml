server:
  port: 24130

spring:
  cassandra:
    port: 9042
    keyspace-name: post_keyspace
    schema-action: CREATE_IF_NOT_EXISTS
    local-datacenter: datacenter1
    contact-points: localhost:9042
    url: jdbc:cassandra://localhost:9042/post_keyspace?localdatacenter=datacenter1
    username: cassandra
    password: cassandra
